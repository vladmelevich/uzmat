# Настройка Cloudflare для домена uzmat.uz (HTTP режим)

## Шаги настройки

### 1. Настройка DNS записей в Cloudflare

1. Зайдите в панель Cloudflare для домена `uzmat.uz`
2. Перейдите в раздел **DNS** → **Records**
3. Добавьте/обновите следующие записи:

   **A запись для основного домена:**
   - **Type:** A
   - **Name:** @ (или uzmat.uz)
   - **IPv4 address:** `109.199.127.149` (IP вашего сервера)
   - **Proxy status:** 🟠 Proxied (включен - оранжевое облачко)
   - **TTL:** Auto

   **A запись для www поддомена:**
   - **Type:** A
   - **Name:** www
   - **IPv4 address:** `109.199.127.149` (IP вашего сервера)
   - **Proxy status:** 🟠 Proxied (включен - оранжевое облачко)
   - **TTL:** Auto

### 2. Настройка SSL/TLS режима

1. Перейдите в раздел **SSL/TLS** → **Overview**
2. Установите режим **Flexible** (Гибкий)
   - Это означает, что между Cloudflare и вашим сервером будет использоваться HTTP
   - Между пользователями и Cloudflare будет HTTPS (Cloudflare предоставляет бесплатный SSL)
   - Это идеально для вашего случая, так как SSL на сервере еще не настроен

### 3. Настройка других параметров

**SSL/TLS → Edge Certificates:**
- Включите **Always Use HTTPS** (опционально, но рекомендуется)
- Включите **Automatic HTTPS Rewrites**

**Speed → Optimization:**
- Включите **Auto Minify** для CSS, HTML, JavaScript (опционально)

**Caching → Configuration:**
- Установите **Caching Level:** Standard
- **Browser Cache TTL:** Respect Existing Headers

### 4. Проверка работы

После настройки DNS записей подождите несколько минут (обычно 1-5 минут) для распространения DNS.

Затем проверьте:
1. Откройте `http://uzmat.uz` в браузере
2. Откройте `http://www.uzmat.uz` в браузере
3. Оба должны работать и показывать ваш сайт

### 5. Важные замечания

- **Режим Flexible** означает, что Cloudflare будет использовать HTTPS для пользователей, но HTTP для связи с вашим сервером
- Это безопасно, так как трафик между Cloudflare и пользователями зашифрован
- Когда вы будете готовы настроить SSL на сервере, измените режим на **Full** или **Full (strict)**

### 6. После настройки SSL на сервере

Когда вы настроите SSL на сервере (например, через Let's Encrypt):
1. Измените режим SSL/TLS на **Full** или **Full (strict)**
2. Обновите настройки Django:
   - `SESSION_COOKIE_SECURE = True`
   - `CSRF_COOKIE_SECURE = True`
   - Раскомментируйте HTTPS секцию в nginx.conf

## Текущие настройки сервера

- **Nginx:** Настроен для работы с доменом `uzmat.uz` и `www.uzmat.uz` на HTTP
- **Django ALLOWED_HOSTS:** Включает `uzmat.uz` и `www.uzmat.uz`
- **Django CSRF_TRUSTED_ORIGINS:** Включает `http://uzmat.uz` и `http://www.uzmat.uz`

Все готово для работы с доменом через Cloudflare в режиме HTTP!



